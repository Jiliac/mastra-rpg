Codemap generation report — 2026-05-15 10:12 GMT+2
Previous run: 2026-05-11 06:11 GMT+2

Scope of update: Waves 2, 3, and 4 all merged into main since the prior codemap
snapshot. The change footprint exceeds 30% — all five codemaps were rewritten
in place rather than incrementally diffed.

================================================================================
Files added since last scan (Waves 2+3+4)
================================================================================

Wave 2 (vault writes + state):
  src/lib/vault/journal.ts        parseRecentEntries + appendJournal
  src/lib/vault/playtests.ts      appendPlaytest → playtests/<YYYY-MM-DD>.md
  src/lib/vault/stubs.ts          extractExcerpt + createStub (kind excludes 'image')
  src/lib/vault/threads.ts        worldTick — atomic tmp+rename rewrite
  src/lib/vault/turnId.ts         mintTurnId → "turn-NNN"
  src/lib/vault/lock.ts           per-vault globalThis mutex (HMR-safe)
  + sibling *.test.ts for each

Wave 3 (dossier + schemas + media):
  src/lib/dossier.ts              identifyOnStage + buildFactionDossier
                                  + buildNarratorDossier + buildIllustratorDossier
                                  + extractVisualBlock
  src/lib/schemas.ts              FactionOutput, NarratorOutput, IllustratorOutput, ImageMeta
                                  (Zod schemas + co-named TS type aliases)
  src/lib/media/image.ts          OpenAI gpt-image-2 PNG generator
  src/lib/media/tts.ts            Inworld inworld-tts-2 OGG Opus renderer
  + sibling *.test.ts for each

Wave 4 (Mastra tools + agents):
  src/mastra/tools/dice.ts        NdM/pbta/advantage/disadvantage parser+roller
  src/mastra/tools/loadEntity.ts  ENOENT-safe vault entity loader
  src/mastra/tools/image.ts       Adapter over src/lib/media/image.ts
  src/mastra/agents/narrator.ts   tools: loadEntity + dice; schema: NarratorOutput
  src/mastra/agents/faction.ts    no tools; schema: FactionOutput
  src/mastra/agents/illustrator.ts tools: image; schema: IllustratorOutput
  + sibling *.test.ts for each

================================================================================
Files removed
================================================================================

  src/mastra/agents/weather-agent.ts      (Wave 4 cleanup, c132a4a)
  src/mastra/tools/weather-tool.ts        (Wave 4 cleanup, c132a4a)
  src/mastra/workflows/weather-workflow.ts (Wave 4 cleanup, c132a4a)
  src/mastra/workflows/                    directory removed
  @mastra/memory                           dependency dropped (was used only by weather-agent)

================================================================================
Files modified
================================================================================

  src/mastra/index.ts
    - Now registers agents: { narratorAgent, factionAgent, illustratorAgent }
    - workflows key intentionally omitted (was `weatherWorkflow`); Wave 5 will reintroduce
    - Storage, logger, observability config unchanged
  src/mastra/index.test.ts
    - Verifies registration of the three Wave-4 agents
    - Asserts weatherAgent / weatherWorkflow absent
  package.json
    - Added: @inworld/tts ^1.1.1, openai ^6.37.0
    - Removed: @mastra/memory

================================================================================
Latent issues surfaced
================================================================================

  src/app/api/chat/route.ts (both POST and GET)
    Still references agentId: 'weather-agent' and mastra.getAgentById('weather-agent').
    The weather agent no longer exists in the runtime — calls to /api/chat will
    fail at runtime. The route is dead code until Wave 5 lands /api/turn.
    Flagged in architecture.md, backend.md, frontend.md.

================================================================================
New external services / env vars
================================================================================

  +OpenAI Images        gpt-image-2 (quality=high) via image tool                OPENAI_API_KEY
  +Inworld TTS          inworld-tts-2 OGG Opus via src/lib/media/tts.ts          INWORLD_API_KEY
  +VAULT_SLUG env var   default vault root for loadEntity + image tools

  -open-meteo (geocoding + forecast) — removed with weather tool

================================================================================
Architecture changes
================================================================================

  - Mastra runtime is no longer a starter weather demo; it is the RPG agent
    suite (narrator + faction + illustrator) registered against Wave-1/2/3 vault
    plumbing.
  - The turn-orchestrating workflow (src/mastra/workflows/turn.ts) is the only
    remaining piece before /api/turn can replace /api/chat — Wave 5 ticket.
  - On-stage detection + dossier assembly is now a pure-TS layer in src/lib/
    rather than embedded in agent prompts. The agents call it via the workflow
    (when written); none of the three agents construct prompts themselves.

================================================================================
Staleness signals
================================================================================

  - /api/chat route: stale by ~4 days; carries a broken agentId reference.
  - chat/page.tsx: still wired to /api/chat; needs rewire to /api/turn in Wave 6.
  - No codemap has gone 90+ days without update — all five regenerated today.
